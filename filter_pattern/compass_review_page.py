"""Interactive review page; state colours never use future return labels."""

PAGE = r'''<!doctype html><html lang="vi"><head><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1">
<title>Market Compass D1 · Review 7 mã</title>
<style>
:root{color-scheme:dark;--bg:#0a111e;--panel:#101b2c;--line:#283b52;--text:#e3edf8;--muted:#9eafc4;--blue:#38bdf8;--green:#55dca3;--orange:#fb923c;--red:#f87171}*{box-sizing:border-box}body{margin:0;background:var(--bg);color:var(--text);font:14px system-ui,sans-serif}main{max-width:1500px;margin:auto;padding:28px 24px 44px}h1{font-size:27px;letter-spacing:-.6px;margin:8px 0}h2{font-size:19px;margin:0}h3{margin:0;font-size:17px}p{color:var(--muted);line-height:1.65}a{color:#7bd5ff}button,select,input{font:inherit}select,button{background:#16243a;color:var(--text);border:1px solid #40556e;padding:8px 10px;border-radius:6px}button{cursor:pointer}button:hover{border-color:var(--blue)}:focus-visible{outline:2px solid var(--blue);outline-offset:3px}.eyebrow{color:var(--blue);font-size:11px;font-weight:700;letter-spacing:2px}.header,.section-head,.price-head{display:flex;align-items:center;justify-content:space-between;gap:14px;flex-wrap:wrap}.badge{border:1px solid #415776;border-radius:20px;padding:6px 12px;font-size:12px}.controls{display:flex;align-items:center;flex-wrap:wrap;gap:16px;padding:15px 0}.controls label{display:flex;align-items:center;gap:8px;color:var(--muted)}#asof{min-width:140px}.replay{display:flex;align-items:center;gap:10px;flex:1;min-width:230px}#replay{width:100%}.notice{padding:12px 16px;background:#252219;border:1px solid #635b33;border-radius:8px;color:#d7cfae;font-size:12px}.cards{display:grid;grid-template-columns:repeat(7,minmax(0,1fr));gap:9px;margin:18px 0}.card{text-align:left;padding:12px;background:var(--panel);border-top:3px solid var(--line);min-width:0}.card b,.card small,.card span{display:block}.card b{font-size:14px}.card small{font-size:10px;color:var(--muted);margin:6px 0;min-height:26px;line-height:1.3}.card span{font-size:10px;letter-spacing:.2px}.panel{background:var(--panel);border:1px solid var(--line);border-radius:10px;padding:18px;margin-top:20px}.sub{font-size:12px;color:var(--muted);margin:8px 0 16px}.kpis{display:grid;grid-template-columns:repeat(4,1fr);gap:10px;margin:18px 0}.kpi{border:1px solid var(--line);border-radius:8px;padding:14px}.kpi small{display:block;color:var(--muted);font-size:11px}.kpi strong{font-size:26px;font-weight:600;display:block;margin-top:6px}.kpi em{font-size:11px;color:var(--muted);font-style:normal;display:block;margin-top:4px}.chart-scroll,.table-scroll{overflow-x:auto}svg{display:block;width:100%}#performance{min-width:560px}#compass{min-width:680px;background:#0b1525;border-radius:7px}.legend{display:flex;gap:18px;flex-wrap:wrap;font-size:12px;color:var(--muted);margin:14px 0}.legend span{display:flex;align-items:center;gap:7px}.dot{width:8px;height:8px;border-radius:50%;display:inline-block}table{border-collapse:collapse;width:100%;font-size:12px}th,td{padding:12px 10px;text-align:right;white-space:nowrap;border-bottom:1px solid var(--line)}th{color:var(--muted);font-weight:500}td:first-child,th:first-child{text-align:left}tbody tr:hover{background:#192940}.price-grid{display:grid;grid-template-columns:repeat(2,minmax(0,1fr));gap:16px;margin-top:16px}.price-card{padding:15px;border:1px solid var(--line);border-radius:8px;background:#0c1626;min-width:0}.price-card svg{min-width:480px;touch-action:pan-y}.price-card .source{color:var(--muted);font-size:11px;margin:5px 0}.price-card .readout{font-size:11px;min-height:32px;margin:8px 0 0;font-variant-numeric:tabular-nums}.phase-badge{font-size:11px}.price-grid.single{grid-template-columns:1fr}.footer{font-size:12px;margin-top:24px}details{margin-top:16px}summary{cursor:pointer;padding:8px 0;color:var(--muted)}[hidden]{display:none!important}@media(max-width:1100px){.cards{grid-template-columns:repeat(4,minmax(0,1fr))}}@media(max-width:750px){main{padding:18px 12px}.price-grid{grid-template-columns:1fr}.panel{padding:12px}.kpis{grid-template-columns:repeat(2,1fr)}.cards{grid-template-columns:repeat(2,minmax(0,1fr))}h1{font-size:23px}.controls{gap:10px}.replay{flex-basis:100%}}
</style></head><body><main>
<header class="header"><div><div class="eyebrow">MARKET OVERVIEW / D1</div><h1>La bàn thị trường · review 7 mã</h1><p class="sub">Bản ổn định hiện tại và bản trước khi làm mượt. Một quy tắc chung cho cả rổ.</p></div><span class="badge">Nghiên cứu hồi cứu · mục tiêu 70% chưa đạt</span></header>
<div class="controls"><label>Phiên bản <select id="model"><option value="wave">Hiện tại · ổn định</option><option value="original">Trước khi làm mượt</option></select></label><label>Ngày <input id="asof" type="date"></label><div class="replay"><input id="replay" aria-label="Phát lại theo ngày" type="range" min="0" step="1"><button id="latest">Mới nhất</button></div></div>
<div class="notice">XAUUSD sử dụng <b>GC=F — vàng futures đại diện</b>, đúng nguồn OHLC của scanner trong repo. Sáu mã còn lại giữ dữ liệu đã kiểm tra. Bảy mã hiển thị không đồng nghĩa đã có OHLC XAUUSD spot.</div>
<div id="cards" class="cards"></div>
<section class="panel" aria-labelledby="performance-title"><div class="section-head"><h2 id="performance-title">Hiệu suất định hướng tương lai</h2><label>Sau <select id="horizon"><option value="5">5 nến D1</option><option value="10" selected>10 nến D1</option><option value="20">20 nến D1</option></select></label></div>
<p id="metric-scope" class="sub"></p><div class="kpis" id="kpis"></div>
<div class="legend"><span><i class="dot" style="background:#38bdf8"></i>Tỷ lệ đúng hướng la bàn</span><span><i class="dot" style="background:#cbd5e1"></i>Đối chứng luôn tăng, cùng ngày</span><span style="color:#f6c76b">┆ Mốc mục tiêu 70%</span></div>
<div class="chart-scroll"><svg id="performance" viewBox="0 0 1000 320" role="img" aria-label="Tỷ lệ đúng hướng theo mã so với mục tiêu 70 phần trăm"></svg></div>
<div class="table-scroll"><table><thead><tr><th>Mã / nguồn</th><th>Đúng hướng</th><th>Độ phủ</th><th>Đúng / mẫu</th><th>Sai</th><th>Tăng: đúng / mẫu</th><th>Giảm: đúng / mẫu</th><th>Đổi màu</th></tr></thead><tbody id="performance-rows"></tbody></table></div>
<p class="sub">Đúng = dấu xu hướng trục X tại đóng nến T khớp return từ open T+1 đến close T+h. Giá không đổi tính sai. Đây không phải lợi nhuận giao dịch. Chỉ tính nhãn đã hoàn tất tại ngày đang xem; các mẫu có chồng lấn.</p></section>
<section class="panel"><div class="section-head"><h2>La bàn bốn trạng thái</h2><span class="sub">12 nến gần nhất · mũi tên chỉ hướng dịch chuyển</span></div><div class="legend" id="phase-legend"></div><div class="chart-scroll"><svg id="compass" viewBox="0 0 1100 440" role="img" aria-label="La bàn bốn trạng thái cho bảy mã"></svg></div><p id="axis-note" class="sub"></p></section>
<section class="panel"><div class="section-head"><h2>Giá thực tế và màu trạng thái</h2><div class="controls"><label>Hiển thị <select id="symbol"><option value="ALL">Tất cả 7 mã</option></select></label><label>Khoảng xem <select id="window"><option value="60">60 nến</option><option value="180" selected>180 nến</option><option value="360">360 nến</option><option value="all">Toàn kỳ</option></select></label></div></div><p class="sub">Đường close, thang log. Màu dùng trạng thái tại ngày đó, không tô theo kết quả tương lai. Improving vẫn thuộc xu hướng giảm; Weakening vẫn thuộc xu hướng tăng. Di chuột, chạm hoặc dùng phím ← → để xem từng nến.</p><div id="prices" class="price-grid"></div></section>
<details><summary>Cách đọc và các vòng thử gần nhất</summary><p>Các bộ lọc và giả thuyết hồi phục chưa vượt kiểm tra mục tiêu nên chưa thay phiên bản hiện tại. Hiệu suất là đánh giá hồi cứu từ 2024, trên dữ liệu đã xem ở nhiều vòng. Tổng rổ cân bằng tài sản, US500 và SPY mỗi mã nửa trọng số. Không coi các mã tương quan là các bằng chứng độc lập.</p><p><a href="../compass-gold-repo-d1/index.html">Các thử nghiệm, đã thêm nguồn vàng repo</a> · <a href="../compass-reversion-d1/index.html">Thử nghiệm nhịp hồi</a> · <a href="../rrg-comparison-d1/index.html">Đối chiếu RRG gốc</a></p></details>
<p class="footer"><a href="results.json">Dữ liệu, nguồn và số liệu đầy đủ</a> · <a href="../compass-rotation-stable-d1/index.html">Bản review sáu mã trước</a> · Chưa thay RRG hoặc scanner.</p>
</main><script>
'use strict';
const D=__DATA__,$=id=>document.getElementById(id),symbols=Object.keys(D.assets),dates=[...new Set(symbols.flatMap(s=>D.assets[s].history.map(r=>r.date)))].sort();
const colors={LEADING:'#55dca3',WEAKENING:'#fb923c',LAGGING:'#f87171',IMPROVING:'#38bdf8',CENTER:'#8794aa'},pct=v=>v==null?'—':(v*100).toFixed(1)+'%',num=v=>v.toLocaleString('en-US',{maximumFractionDigits:2});
const make=(tag,attrs,parent)=>{const e=document.createElementNS('http://www.w3.org/2000/svg',tag);for(const[k,v]of Object.entries(attrs))e.setAttribute(k,v);parent.append(e);return e;},text=(value,attrs,parent)=>{const e=make('text',attrs,parent);e.textContent=value;return e;};
for(const s of symbols)$('symbol').add(new Option(s==='XAUUSD'?'XAUUSD · GC=F proxy':s,s));
for(const [s,label]of [['IMPROVING','Giảm, động lượng cải thiện'],['LEADING','Tăng, động lượng mạnh lên'],['WEAKENING','Tăng, động lượng yếu đi'],['LAGGING','Giảm, động lượng yếu thêm']]){const e=document.createElement('span'),dot=document.createElement('i');dot.className='dot';dot.style.background=colors[s];e.append(dot,s+' · '+label);$('phase-legend').append(e);}
$('replay').max=dates.length-1;$('replay').value=dates.length-1;$('asof').min=dates[0];$('asof').max=dates.at(-1);$('asof').value=dates.at(-1);
function rows(s){return D.assets[s].history.filter(r=>r.date<=$('asof').value);}
function stats(history,m,h){const eligible=history.filter(r=>r.outcomes[h]&&r.outcomes[h].exit_date<=$('asof').value),chosen=eligible.filter(r=>r.forecasts[m].trend_sign!==0),wins=chosen.filter(r=>r.outcomes[h].return_pct*r.forecasts[m].trend_sign>0),side=sign=>{const a=chosen.filter(r=>r.forecasts[m].trend_sign===sign);return {n:a.length,w:a.filter(r=>r.outcomes[h].return_pct*sign>0).length};};let changes=0;for(let i=1;i<history.length;i++)if(history[i].forecasts[m].phase!==history[i-1].forecasts[m].phase)changes++;return {eligible:eligible.length,n:chosen.length,w:wins.length,accuracy:chosen.length?wins.length/chosen.length:null,coverage:eligible.length?chosen.length/eligible.length:0,up:side(1),down:side(-1),always:eligible.length?eligible.filter(r=>r.outcomes[h].return_pct>0).length/eligible.length:null,changes};}
function performance(m){const h=$('horizon').value,statsBySymbol=Object.fromEntries(symbols.map(s=>[s,stats(rows(s),m,h)]));let total=0,chosen=0,wins=0,n=0,w=0,eligible=0;for(const[s,a]of Object.entries(statsBySymbol)){if(!a.eligible)continue;const weight=['US500','SPY'].includes(s)?.5:1;total+=weight;chosen+=weight*a.coverage;wins+=weight*a.w/a.eligible;n+=a.n;w+=a.w;eligible+=a.eligible;}
const accuracy=chosen?wins/chosen:null;$('kpis').replaceChildren();for(const[label,value,note]of [['ĐÚNG HƯỚNG · CẢ RỔ',pct(accuracy),'Có trọng số, gồm vàng GC=F'],['ĐỘ PHỦ',pct(total?chosen/total:0),'Trên '+eligible.toLocaleString()+' ngày-tài sản đủ kết quả'],['TÍN HIỆU ĐƯỢC CHẤM',n.toLocaleString(),w.toLocaleString()+' đúng · '+(n-w).toLocaleString()+' sai (đếm mẫu)'],['MỐC MỤC TIÊU','70%','Không phải xác suất được bảo đảm']]){const e=document.createElement('div');e.className='kpi';for(const[tag,value2]of [['small',label],['strong',value],['em',note]]){const el=document.createElement(tag);el.textContent=value2;e.append(el);}$('kpis').append(e);}
$('metric-scope').textContent='Từ '+dates[0]+' đến '+$('asof').value+' · '+D.models[m]+' · kết quả sau '+h+' nến. Đổi khoảng xem giá không thay đổi kỳ đánh giá.';
const svg=$('performance');svg.replaceChildren();const x=v=>160+v*690;for(let v=0;v<=1;v+=.2){make('line',{x1:x(v),x2:x(v),y1:12,y2:278,stroke:'#26394f'},svg);text(Math.round(v*100)+'%',{x:x(v),y:306,fill:'#9eafc4','font-size':12,'text-anchor':'middle'},svg);}make('line',{x1:x(.7),x2:x(.7),y1:10,y2:280,stroke:'#f6c76b','stroke-dasharray':'5 4'},svg);text('70%',{x:x(.7)+5,y:12,fill:'#f6c76b','font-size':11},svg);
$('performance-rows').replaceChildren();symbols.forEach((s,i)=>{const a=statsBySymbol[s],y=32+i*35;const group=make('g',{'data-symbol':s,'data-accuracy':a.accuracy??''},svg);text(s+(s==='XAUUSD'?' *':''),{x:146,y:y+4,fill:'#dce8f6','font-size':13,'text-anchor':'end'},group);if(a.accuracy!=null)make('rect',{x:x(0),y:y-9,width:a.accuracy*690,height:18,fill:'#38bdf8',opacity:.8,rx:3},group);if(a.always!=null)make('circle',{cx:x(a.always),cy:y,r:4,fill:'#cbd5e1',stroke:'#101b2c','stroke-width':2},group);text(pct(a.accuracy),{x:873,y:y+4,fill:'#dce8f6','font-size':13},group);const tr=document.createElement('tr');tr.dataset.symbol=s;tr.dataset.signals=a.n;const side=v=>pct(v.n?v.w/v.n:null)+' / '+v.n;for(const value of [s+(s==='XAUUSD'?' · GC=F':''),pct(a.accuracy),pct(a.coverage),a.w+' / '+a.n,a.n-a.w,side(a.up),side(a.down),a.changes]){const td=document.createElement('td');td.textContent=value;tr.append(td);}$('performance-rows').append(tr);});return statsBySymbol;}
function compass(m){const svg=$('compass');svg.replaceChildren();const tails=symbols.map(s=>({s,rows:rows(s).slice(-12)})).filter(a=>a.rows.length),L=65,R=1040,T=25,B=387,CX=(L+R)/2,CY=(T+B)/2;
const xmax=Math.max(.5,...tails.flatMap(a=>a.rows.map(r=>Math.abs(r.forecasts[m].score)*1.25))),ymax=Math.max(.2,...tails.flatMap(a=>a.rows.map(r=>Math.abs(r.forecasts[m].momentum)*1.3))),x=v=>CX+v/xmax*(R-L)/2,y=v=>CY-v/ymax*(B-T)/2;
for(const[xx,yy,c]of [[L,T,'#102b43'],[CX,T,'#103024'],[L,CY,'#321c29'],[CX,CY,'#33271d']])make('rect',{x:xx,y:yy,width:(R-L)/2,height:(B-T)/2,fill:c},svg);
if(m==='wave'){make('rect',{class:'confirmation-band',x:x(-.1),y:T,width:x(.1)-x(-.1),height:B-T,fill:'#cbd5e1',opacity:.09},svg);make('rect',{class:'confirmation-band',x:L,y:y(.05),width:R-L,height:y(-.05)-y(.05),fill:'#cbd5e1',opacity:.09},svg);}
for(let i=-2;i<=2;i++){make('line',{x1:x(i*xmax/2),x2:x(i*xmax/2),y1:T,y2:B,stroke:i===0?'#8a9bb0':'#293e50'},svg);make('line',{x1:L,x2:R,y1:y(i*ymax/2),y2:y(i*ymax/2),stroke:i===0?'#8a9bb0':'#293e50'},svg);text((i*xmax/2).toFixed(2),{x:x(i*xmax/2),y:412,fill:'#9eafc4','font-size':11,'text-anchor':'middle'},svg);text((i*ymax/2).toFixed(2),{x:55,y:y(i*ymax/2)+4,fill:'#9eafc4','font-size':11,'text-anchor':'end'},svg);}
for(const[t,xx,yy]of [['IMPROVING',80,47],['LEADING',950,47],['LAGGING',80,370],['WEAKENING',930,370]])text(t,{x:xx,y:yy,fill:'#dce5f0','font-size':12},svg);
const defs=make('defs',{},svg),occupied=[];tails.forEach((a,i)=>{const last=a.rows.at(-1),f=last.forecasts[m],c=colors[f.phase],group=make('g',{'data-symbol':a.s,'data-date':last.date,'data-phase':f.phase},svg),marker=make('marker',{id:'arrow'+i,viewBox:'0 0 10 10',refX:9,refY:5,markerWidth:6,markerHeight:6,orient:'auto'},defs);make('path',{d:'M0 0 L10 5 L0 10 Z',fill:c},marker);make('polyline',{points:a.rows.map(r=>x(r.forecasts[m].score)+','+y(r.forecasts[m].momentum)).join(' '),fill:'none',stroke:c,'stroke-width':2,opacity:.5},group);if(a.rows.length>1){const p=a.rows.at(-2).forecasts[m];make('line',{x1:x(p.score),y1:y(p.momentum),x2:x(f.score),y2:y(f.momentum),stroke:c,'stroke-width':3,'marker-end':'url(#arrow'+i+')'},group);}make('circle',{cx:x(f.score),cy:y(f.momentum),r:4,fill:c},group);let lx=Math.min(950,Math.max(80,x(f.score)+9)),ly=Math.min(355,Math.max(65,y(f.momentum)-10));let attempts=0;while(occupied.some(p=>Math.abs(p.x-lx)<110&&Math.abs(p.y-ly)<18)&&attempts++<18)ly=ly+20>355?65:ly+20;occupied.push({x:lx,y:ly});if(Math.abs(ly-y(f.momentum))>20)make('line',{x1:x(f.score),y1:y(f.momentum),x2:lx,y2:ly-4,stroke:c,opacity:.5},group);text(a.s,{x:lx,y:ly,fill:c,'font-size':13,'paint-order':'stroke',stroke:'#0a1424','stroke-width':3},group);make('title',{},group).textContent=a.s+' · '+D.assets[a.s].source+' · '+last.date+' · '+f.phase+' · X '+f.score.toFixed(3)+' / Y '+f.momentum.toFixed(3);});
$('axis-note').textContent=m==='wave'?'X = EMA5(độ dốc EMA20 qua 3 nến / ATR14); Y = EMA3(thay đổi X qua 5 nến). Vùng đệm X ±0,10 / Y ±0,05 giữ màu cũ; tọa độ vẫn là giá trị thật. Đây là xu hướng tuyệt đối, không phải JdK RRG so với benchmark.':'X = độ dốc EMA20 qua 3 nến / ATR14; Y = thay đổi X qua 5 nến. Bản cũ đổi màu tại 0.';}
function prices(m){const root=$('prices');root.replaceChildren();const visible=$('symbol').value==='ALL'?symbols:[$('symbol').value];root.classList.toggle('single',visible.length===1);
for(const s of visible){const all=rows(s),points=$('window').value==='all'?all:all.slice(-Number($('window').value)),last=points.at(-1),card=document.createElement('article');card.className='price-card';card.dataset.symbol=s;const head=document.createElement('div');head.className='price-head';const title=document.createElement('h3');title.textContent=s;const badge=document.createElement('span');badge.className='phase-badge';badge.textContent=last?.forecasts[m].phase||'Chưa có dữ liệu';badge.style.color=colors[last?.forecasts[m].phase];head.append(title,badge);const source=document.createElement('div');source.className='source';source.textContent=D.assets[s].source+(last?' · '+points[0].date+' → '+last.date:'');const scroll=document.createElement('div');scroll.className='chart-scroll';const svg=make('svg',{viewBox:'0 0 660 252',role:'img',tabindex:0,'aria-label':'Giá D1 '+s+' và bốn màu trạng thái'},scroll),readout=document.createElement('p');readout.className='readout';readout.setAttribute('aria-live','polite');card.append(head,source,scroll,readout);root.append(card);if(!points.length)continue;
const L=72,R=642,T=16,B=194,logs=points.map(r=>Math.log(r.close)),low=Math.min(...logs),high=Math.max(...logs),padding=Math.max((high-low)*.08,.002),lo=low-padding,hi=high+padding,x=i=>points.length===1?(L+R)/2:L+i*(R-L)/(points.length-1),y=v=>B-(v-lo)/(hi-lo)*(B-T);
for(let j=0;j<4;j++){const v=lo+(hi-lo)*j/3;make('line',{x1:L,x2:R,y1:y(v),y2:y(v),stroke:'#25384d'},svg);text(num(Math.exp(v)),{x:L-8,y:y(v)+4,fill:'#9eafc4','font-size':10,'text-anchor':'end'},svg);}
const step=points.length>1?(R-L)/(points.length-1):12;points.forEach((r,i)=>{const phase=r.forecasts[m].phase,c=colors[phase];if(i&&r.index===points[i-1].index+1)make('line',{class:'price-segment','data-date':r.date,'data-phase':phase,x1:x(i-1),y1:y(logs[i-1]),x2:x(i),y2:y(logs[i]),stroke:c,'stroke-width':2.2},svg);make('rect',{class:'price-state','data-date':r.date,'data-phase':phase,x:Math.max(L,x(i)-step/2),y:206,width:Math.max(.5,Math.min(R,x(i)+step/2)-Math.max(L,x(i)-step/2)),height:5,fill:c},svg);});
for(const i of [...new Set([0,Math.floor((points.length-1)/2),points.length-1])])text(points[i].date,{x:x(i),y:237,fill:'#9eafc4','font-size':11,'text-anchor':i===0?'start':i===points.length-1?'end':'middle'},svg);
const hover=make('g',{'pointer-events':'none'},svg);let active=points.length-1;function inspect(i){active=Math.min(points.length-1,Math.max(0,i));const r=points[active],f=r.forecasts[m];hover.replaceChildren();make('line',{x1:x(active),x2:x(active),y1:T,y2:211,stroke:'#cbd5e1','stroke-dasharray':'3 4',opacity:.5},hover);make('circle',{cx:x(active),cy:y(logs[active]),r:3.5,fill:colors[f.phase]},hover);readout.textContent=r.date+' · Close '+num(r.close)+' · '+f.phase+(f.within_buffer?' · Trong vùng đệm':'');readout.style.color=colors[f.phase];svg.dataset.inspectedDate=r.date;}
svg.addEventListener('pointermove',e=>{const p=svg.createSVGPoint();p.x=e.clientX;p.y=e.clientY;const local=p.matrixTransform(svg.getScreenCTM().inverse());inspect(Math.round((local.x-L)/(R-L)*(points.length-1)));});svg.addEventListener('keydown',e=>{if(e.key==='ArrowLeft'||e.key==='ArrowRight'){e.preventDefault();inspect(active+(e.key==='ArrowRight'?1:-1));}});inspect(active);}}
function draw(){const m=$('model').value;$('cards').replaceChildren();for(const s of symbols){const last=rows(s).at(-1),f=last?.forecasts[m],card=document.createElement('button');card.className='card';card.dataset.symbol=s;card.dataset.phase=f?.phase||'MISSING';card.style.borderTopColor=colors[f?.phase]||'#536174';for(const[tag,value]of [['b',s],['small',D.assets[s].source],['span',f?f.phase+' · '+last.date:'Chưa có dữ liệu']]){const el=document.createElement(tag);el.textContent=value;if(tag==='span')el.style.color=colors[f?.phase];card.append(el);}card.onclick=()=>{$('symbol').value=s;prices(m);$('prices').scrollIntoView({behavior:'smooth',block:'start'});};$('cards').append(card);}performance(m);compass(m);prices(m);}
$('replay').oninput=()=>{$('asof').value=dates[Number($('replay').value)];draw();};$('asof').onchange=()=>{const value=$('asof').value;const i=Math.max(0,dates.findLastIndex(d=>d<=value));$('replay').value=i;$('asof').value=dates[i];draw();};$('latest').onclick=()=>{$('replay').value=dates.length-1;$('asof').value=dates.at(-1);draw();};$('model').onchange=draw;$('horizon').onchange=()=>performance($('model').value);$('symbol').onchange=$('window').onchange=()=>prices($('model').value);draw();
</script></body></html>'''
